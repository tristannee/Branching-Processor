000a # Register 0 is 10
0000 # next one
8000
0001
ffff
ffff
ffff

# This program would enter an infinite loop if it weren't for our error-checking. 
# This is because in our while loop, remainder never decreases since divisor is 0 so remainder 
# is always greater than 0 which means we would never reach the break statement.
